
<!--<%#= form_tag contracts_path, :method => :get do %>-->
<!--  <p>-->
<!--    <#= text_field_tag :search, params[:search] %>-->
<!--    <%#= submit_tag "Search", :last_name => nil %>-->
<!--<%# end %>-->
<%#= link_to "Calendar", calendar_path %>
<div class="clearfix container">
    <div class="page-header row row-same-height"><h4 class="col-sm-12 col-xs-height text-uppercase">Upcoming Events</h4> </div><h5></h5>
      <hr>
  <% contract.each do |contract| %>
    <div class="sheet-inner m20 text-gray-50">
      <div class="blue-a200 text-gray-50">
        <%= link_to "#{contract.date_of_event.strftime("%m/%d/%y")}", contract %>
      </div>
    
  



<%= contract.act_booked %>
<%= link_to "#{contract.first_name} #{contract.last_name}", contract %>
<%#= contract.contract_status %>

<%= contract.type_of_event %>
<%#= contract.location_city %>
<%= contract.eventtime %>


    <% if contract.confirmed %>fhfhfhf
    <%= button_to 'Get Info', contract, class: 'btn btn-flat btn-sm btn-success',method: :get %>
    <% else %>
    <%#= button_to 'CONFIRM', m, class: 'btn-danger',method: :get %>
    <%= button_to 'CONFIRM', contract_path(id: contract.id), class: "btn btn-flat btn-sm btn-danger" %>
    <% end %>
    <%#= link_to 'Get Info', contract, class: 'button blue' %>

</div>
<% end %>
</div>
